# OCR Service dependencies
# Install with: pip install -r requirements.txt

# FastAPI web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Image processing
Pillow>=10.0.0
opencv-python-headless>=4.9.0

# PaddleOCR
# Note: paddlepaddle must match your system (CPU or GPU)
# For CPU: paddlepaddle>=2.6.0
# For CUDA 11.x: paddlepaddle-gpu>=2.6.0
# For CUDA 12.x: paddlepaddle-gpu>=3.0.0
paddlepaddle>=2.6.0
paddleocr>=2.8.0

# Utilities
numpy>=1.24.0
pydantic>=2.0.0
